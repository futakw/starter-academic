# Experience Logos

Put officially distributable organization logos in this directory.

Examples:
- `nec.svg`
- `cyberagent.svg`

Then map the logo path in `portfolio/layouts/partials/custom_head.html` inside `initExperienceLogos()`.
Only mapped hosts are rendered.
